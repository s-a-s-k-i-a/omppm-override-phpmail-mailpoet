# Setup audit — issue #10

Audited original `origin/main` 8fb5a17 and the existing CI receipt
[34682597638](https://github.com/s-a-s-k-i-a/omppm-override-phpmail-mailpoet/actions/runs/34682597638).
This separates repository setup defects from inherited product defects; it
is not evidence about the general reliability of a particular agent body.

| Finding | Evidence / effect | Resolution |
| --- | --- | --- |
| Plugin Check was nonblocking | `continue-on-error: true` allowed CI success despite actual escaping, i18n, file-write and outdated tested-version errors | Removed exception; product defects handled under their existing issues |
| Playground lacked completion evidence | Original script returned exit 0 with no visible assertion result; no negative control existed | `runPHP` loads WordPress and asserts alias identity; a fresh external receipt is mandatory; intentional failure must reach its own receipt and fail |
| Release bypassed several CI gates | Release ran only PHP 8.3 unit/lint; no full PHP matrix, Playground, Plugin Check or native send E2E dependency | Release calls the same reusable CI and waits for all jobs before publishing |
| Local Node was unconstrained | Node 26.5.0 failed to load Playground's fs-ext native module on arm64 | Script invokes pinned Node 22.16.0 and Playground 3.1.46 via npm |
| Stale build example | AGENTS instructed version 1.2.4 despite main 1.2.5 | Build example derives the source version |
| Tools alias status was misleading (imported product defect) | Existence of native PHPMail was enough to display active; autoload could trigger native load | #3: non-autoloading existence plus Reflection identity; unit positive and native-class negative tests |
| Admin output and notifications were unescaped (imported) | Plugin Check security errors and JS HTML interpolation | #3: context-appropriate escaping, request unslashing, text-only notifications |
| Clear Logs truncated shared WordPress log (imported) | Direct write to WP_CONTENT_DIR/debug.log, including unrelated plugin entries | #4: private bounded database events; clearing touches only the plugin option; no historical logs modified |
| Translatable test mail was malformed (imported) | Concatenated translation string and missing placeholder comments | #2: one literal and numbered placeholders/comments; full WPCS adoption remains separate |

## Receipts and limits

Local setup branch: unit tests 17/47 assertions; PHP compatibility lint;
admin security/nonce/input/i18n sniffs; PHP syntax; release content guards and
ZIP build passed. Playground controls passed both genuine WordPress alias
assertions and deliberate failure. The assertion is an alias smoke only;
it does not prove newsletter sends or MailPoet database compatibility. Those
belong to the native MySQL + real SMTP catcher E2E matrix under #8.

GitHub API readback on 2026-09-12: public repo, default branch main; main
branch protection endpoint returned 404. `wordpress.org` environment exists,
requires reviewer `s-a-s-k-i-a`, permits self-review and administrator bypass,
and has no deployment-branch restriction. Secret names SVN_USERNAME and
SVN_PASSWORD exist; values were not accessed. These are audit-time facts;
subsequent protection changes require their own readback.

The final integrated PR/Main CI, native E2E, release tag, SVN and public ZIP
receipts are deliberately not claimed by this local audit.

## Applied main-branch protection

After integrating the checks, GitHub API readback confirmed strict required
statuses for PHP 8.1/8.3/8.4, WordPress Playground, WordPress Plugin Check and
MailPoet SMTP E2E on `main`. Administrator enforcement and conversation
resolution are enabled; force pushes and branch deletion are prohibited.
The existing protected `wordpress.org` deployment approval was preserved.
